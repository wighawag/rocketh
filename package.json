{
  "name": "rocketh-monorepo",
  "private": true,
  "version": "0.5.0",
  "homepage": "https://github.com/wighawag/rocketh",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wighawag/rocketh.git"
  },
  "bugs": {
    "url": "https://github.com/wighawag/rocketh/issues"
  },
  "type": "module",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@nx/js": "^22.6.5",
    "@types/node": "^25.6.0",
    "dorfl": "0.8.0",
    "ezx": "^0.0.18",
    "nx": "^22.6.5",
    "prettier": "^3.8.3",
    "set-defaults": "^0.0.5",
    "syncpack": "^14.3.0",
    "tsx": "^4.21.0",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.10",
    "zellij-launcher": "^0.0.1"
  },
  "volta": {
    "node": "22.14.0"
  },
  "packageManager": "pnpm@10.28.1",
  "pnpm": {
    "comment": "Security floor for transitive DEV/TEST/DOCS tooling deps flagged by Dependabot. None are runtime deps of the published @rocketh/* packages; they arrive via vitepress, hardhat and lws. Only the entries that still regress to a vulnerable version WITHOUT the override are kept here (the rest are already satisfied by the resolved lockfile). vitepress 1.6.4 and vitest 4.1.10 are already the newest releases, so these floors cannot be removed by upgrading; revisit when those tools ship patched dep trees.",
    "overrides": {
      "axios@>=1.0.0 <1.18.0": "^1.18.0",
      "vite@>=7.0.0 <7.3.5": "^7.3.5",
      "esbuild@<0.25.0": "^0.25.0",
      "esbuild@>=0.27.3 <0.28.1": "^0.28.1",
      "ws@>=8.0.0 <8.21.0": "^8.21.0",
      "adm-zip@<0.6.0": "^0.6.0",
      "@tootallnate/once@<2.0.1": "^2.0.1",
      "@koa/cors@<5.0.0": "^5.0.0"
    }
  },
  "scripts": {
    "prepare": "set-defaults .vscode",
    "prepublishOnly": "pnpm format:check && pnpm build",
    "release": "pnpm prepublishOnly && git push --all && pnpm changeset publish && git push --tags",
    "release:ci": "pnpm prepublishOnly && pnpm changeset publish",
    "changeset:force-patch": "tsx scripts/force-patch-changesets.ts",
    "changeset:version": "pnpm changeset:force-patch && pnpm changeset version",
    "reset": "git clean -fdX; pnpm i",
    "sync-deps": "syncpack fix-mismatches",
    "docs:dev": "vitepress dev",
    "docs:build": "pnpm prepare && vitepress build",
    "docs:preview": "vitepress preview",
    "typecheck": "pnpm -r --parallel exec tsc --noEmit",
    "test:getting-started": "ezx --verbose README.md -s 'Getting Started' && rm -Rf my-rocketh-project",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write \"packages/*/src/**/*.ts\"",
    "format:check": "prettier --check \"packages/*/src/**/*.ts\"",
    "build": "nx run-many -t build",
    "start": "nx run-many -t dev"
  }
}
