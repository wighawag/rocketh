/**
 * `@rocketh/playground/element` has no API: importing it registers the `<rocketh-playground>`
 * custom element as a side effect. Its attributes are documented in the README.
 *
 * This declaration is hand-written rather than emitted, because the entry is built by vite
 * (it compiles `.svelte`) while the rest of the package is built by `tsc`.
 */
export {};
