---
title: Access deployments from companion networks
slug: companion-networks-access
---

# Access deployments from "companion" networks

Provide a way for a deploy script to read deployments from a related ("companion") network — e.g. an L2 deploy script that needs an address deployed on its companion L1.

Captured as a question in the original TODO ("ability to access deployment from companion networks ?"), so the shape and desired semantics are still open.

## Open / unknown

- What defines a "companion" relationship (config? naming convention?).
- Read-only access, or also the ability to deploy across companions.
- How it interacts with the environment/executor and named accounts.

This is a sketch — it needs design before it can be a slice.

_Source: migrated from `TODO.md` during work/ setup._
