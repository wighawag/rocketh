# website-legacy

Parked documentation content migrated from other repositories, kept here verbatim until the rocketh website is redone as a single unified site.

- `hardhat-deploy/` — the docs content (`documentation/`, `index.md`, `.vitepress/config.mts`) copied from the `wighawag/hardhat-deploy` repository, whose website used to be built by rocketh CI. This is raw source to fold into the future unified site; it is not wired into the live rocketh site yet.
