---
---

Test-only: `@rocketh/unknown-signer` pins the hardhat-deploy v1 parity contract of `catchUnknownSigner` in one file (return shape with every key present, `value` always a string, no persisted file or record around a deferring run, loud thunk-misuse errors, the exact printed block).
