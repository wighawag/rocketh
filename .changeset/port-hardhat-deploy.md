---
"hardhat-deploy": minor
---

Move the `hardhat-deploy` package source into the rocketh monorepo (`packages/hardhat-deploy`). It is now built, versioned, and published from here via changesets, with `rocketh` and `@rocketh/node` consumed as workspace dependencies. No API changes for consumers.
