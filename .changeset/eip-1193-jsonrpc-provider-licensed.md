---
'rocketh': patch
'@rocketh/export': patch
---

Require `eip-1193-jsonrpc-provider@^0.4.4`, the first release that declares a licence (MIT). The code is byte-identical to 0.4.3; the bump only makes licence scanners stop reporting it as Unknown.
