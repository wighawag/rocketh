---
---

Remove the dead, commented-out `skip()` scaffolding from the executor (no behaviour change) and document that a hardhat-deploy v1 `skip` export is ignored, with the early-return and run-once replacements.
