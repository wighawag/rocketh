---
---

Add executed hardhat-deploy v1 to rocketh migration pairs to the `hardhat-deploy` test suite (test-only; the published package is unchanged), and rewrite the migration skill's patterns from them, fixing the proxy pattern that taught a `proxyKind` option which does not exist.
