---
---

Governance demo: the README walkthrough now holds end to end on a fresh local node for all five scenario tags. The operator script prints the re-run command with the right `REGISTRY_VERSION`, a new `pnpm advance-time` moves the local node past the timelock delay, and the README states what the first `scenario-ordered` run defers and which half of that scenario's ordering the chain actually enforces.
