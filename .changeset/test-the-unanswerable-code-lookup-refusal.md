---
---

Test + docs only: the interactive deployment path's "the node cannot answer `eth_getCode`" refusal now has a test (it was the only one of the four new refusals with none), and `documentation.md`'s list of deployment failure shapes names it alongside the other three.
