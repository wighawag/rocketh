---
---

Comment-only updates in `@rocketh/playground` and `@rocketh/test-utils`: both described the missing-chain-info notice as a `console.error`, which it no longer is. No behaviour change, so no release. (Safe to leave: the three real changesets alongside it mean the version PR will consume this one too, rather than the empty-changeset publish block described in `CONTEXT.md`.)
