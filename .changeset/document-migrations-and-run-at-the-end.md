---
---

Documentation only: a page for the run-once script mechanism (`id` / `return true` / `.migrations.json`) and `runAtTheEnd`, including the caught-deferral hazard, the batch-proposer pattern a `runAtTheEnd` consumer serves, cross-references from the core-concepts, unknown-signers and captured-transactions pages, and a glossary entry pinning the migration record.
