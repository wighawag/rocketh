---
---

Test-only: `@rocketh/unknown-signer` now runs a mechanically ported hardhat-deploy v1 proxy-upgrade script end to end (deferral in v1 shape, the rest of the script still runs, the re-run after the Safe acts skips the upgrade).
