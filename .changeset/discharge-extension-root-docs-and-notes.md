---
---

Docs and work-tree bookkeeping only: pin the extension-root export constraint in `AGENTS.md` + `CONTEXT.md`, and discharge two observations whose signal has been acted on. No package behaviour changes.
