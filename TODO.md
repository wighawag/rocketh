- [ ] support zksync (once hardhat v3 supports it)
- [ ] ability to access deployment from "companion" networks ?
- [ ] tag tracking: save tags to deployments based exclusively on the tags specified in the script
      Then, if we wanted to reset only certain tags, we still create the dependency graph first
      from that graph we have a list of tag to reset first
